Create a `Conveyor` folder in Workspace with:
- `StartPoint` (Part) — starting location of offers; TODO: position and add moving belt mesh.
- `EndPoint` (Part) — end location; offers are destroyed on arrival.

Place an invisible track between the two to visualize in Studio.
